specVersion: "v1beta"
serviceId: "landing_page"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "landing_page-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
